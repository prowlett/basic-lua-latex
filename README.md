# Basic Lua integration with LuaTeX

Just a quick demo to show a way to use Lua within a LuaTeX document that can be rendered with lualatex. Uses `tex.print` to output a bit of LaTeX code.

Uses package `luacode` because that deals with some special character issues with Lua code in TeX. 
